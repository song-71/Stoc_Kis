# [KIS Open API 문의 첨부] 실시간 장운영정보(H0STMKO0) 응답 필드값 ↔ 개발문서 불일치

- 작성일: 2026-06-26
- 대상 실시간 항목: **[실시간-049] 국내주식 장운영정보(KRX) — TR_ID `H0STMKO0`**
- 수신 환경: 운영 WSS(`ws://ops.koreainvestment.com:21000`) 정상 구독
- 발생 시장 사건(2026-06-26, 유가증권시장/코스피)
  - **11:12:12 매도 사이드카 발동** (코스피200 선물 전일종가 대비 -5%, 약 5분 후 자동 해제)
  - **12:10:12 1단계 서킷브레이커 발동** (코스피 전일종가 대비 -8.19% 급락, 1분 지속, 약 20분간 전 종목 매매거래 중단 → 12:30:12 해제)

---

## 1. 요약

`H0STMKO0` 를 정상 구독 중인 상태에서, **여러 응답 필드의 실제 수신값이 개발문서(개별문서) 코드표와 일치하지 않거나 문서에 정의가 없습니다.** 핵심은 다음과 같습니다.

1. **`MKOP_CLS_CODE`(장운영구분코드)** — 문서에 전혀 없는 **`AF8`·`AF1`·`BF9`** 가 수신됩니다(서킷·VI 단일가 구간). 문서가 정의한 서킷 코드(174/175/184/185)·사이드카 코드(187/388/397/398)는 **수신되지 않았습니다.**
2. **`ISCD_STAT_CLS_CODE`(종목상태구분코드)** — `H0STMKO0` 는 다양한 종목에 대해 **항상 `57`** 을 반환하는데, 동일 종목·동일 시각의 KRX 종목상태(거래소 마스터)는 `00`/`23`/`50`/`76` 등으로 전혀 다릅니다.
3. **`EXCH_CLS_CODE`(거래소구분코드)** — 개발문서에 **가능한 값 목록 자체가 없습니다.**
4. **`ANTC_MKOP_CLS_CODE`(예상장운영구분코드)** — `311`/`112` 가 장중(VI 단일가·서킷) 구간에 수신되나, 문서 설명("장전예상시작/장전예상종료")과 시간대·의미가 맞지 않습니다.
5. 문서 `MKOP_CLS_CODE` 표 자체에 **코드값이 비어 있는 항목 2건**("단일가개시", "서킷브레이커CB 단일가접수")이 존재합니다.

> **클라이언트 파싱 오류 가능성은 배제했습니다.** 응답 컬럼 순서를 문서 Response Body 필드 순서와 동일하게 구성했고, 동일 레코드의 `TRHT_YN` 이 서킷 발동 시 `Y`·해제 시 `N` 으로 정확히 동작하여 필드 정렬에 이상이 없음을 확인했습니다. 즉 `MKOP_CLS_CODE` 필드 자체에 `AF8`/`AF1`/`BF9` 가 수신된 것입니다.

---

## 2. 필드별 대조 (개발문서 기재값 ↔ 2026-06-26 실측값)

| 응답 필드 | 개발문서 기재값(요약) | 실측 수신값(260626) | 일치 여부 / 비고 |
|---|---|---|---|
| `TRHT_YN` (거래정지여부) | Y / N | `Y`(서킷 중), `N`(평시·해제) | 정상 |
| `MKOP_CLS_CODE` (장운영구분코드) | 110·112·121…182·**174/184**(서킷)·**187/388/397/398**(사이드카)·164 등 숫자, `F01`~`F18`, `P01`~`P18` — **A/B 접두 코드 없음** | `None`(평시), **`AF8`**(서킷 발동), **`AF1`**(서킷 해제), **`BF9`**(VI 단일가) | **불일치 — `AF8`/`AF1`/`BF9` 전부 문서 미기재.** 문서상 서킷/사이드카 숫자코드는 미수신 |
| `ANTC_MKOP_CLS_CODE` (예상장운영구분코드) | `112`=장전예상종료, `121`=장후예상시작, `129`=장후예상종료, `311`=장전예상시작 | `311`, `112` (장중 VI·서킷 구간) | 값은 문서에 있으나 **시간대·의미 불일치** |
| `ISCD_STAT_CLS_CODE` (종목상태구분코드) | `51`~`59`, `00` (관리/시장경고/증거금/거래정지/단기 등) | **항상 `57`** | **KRX 종목상태와 불일치** (같은 종목 KRX=`00`/`23`/`50`/`76` 등) — `57` 의 정확한 의미 확인 필요 |
| `VI_CLS_CODE` (VI적용구분코드) | Y / N | `Y`, `N` | 정상 |
| `OVTM_VI_CLS_CODE` (시간외단일가VI) | Y / N | `N` | 정상 |
| `EXCH_CLS_CODE` (거래소구분코드) | **(문서에 값 목록 없음)** | — | **문서에 정의 자체가 없음** — 가능한 값/의미 안내 요청 |
| `MRKT_TRTM_CLS_CODE` (임의연장구분코드) | 1~6 | (당일 미관측) | — |
| `DIVI_APP_CLS_CODE` (동시호가배분처리) | [0]배분개시/해제, [1]매수·매도 상·하한 | (당일 미관측) | — |

---

## 3. 문의 사항

1. **(핵심) `MKOP_CLS_CODE` 의 `AF8`·`AF1`·`BF9` 정의는 무엇입니까?**
   - `AF8`: 12:10 코스피 1단계 서킷브레이커 발동 중(`TRHT_YN=Y`) 수신
   - `AF1`: 12:30 서킷 해제 시(`TRHT_YN=N`) 수신
   - `BF9`: 서킷 이후 12:40~13:48 평시(`TRHT_YN=N`) VI 단일가 추정 구간에 반복 수신
   - 이들 **A/B 접두 코드군의 전체 목록과 의미**, 그리고 문서에 기재된 서킷(174/175/184/185)·사이드카(187/388/397/398) 숫자코드와의 관계(대체/추가/단일가접수 단계 구분 여부)를 알려주시기 바랍니다.

2. **문서 `MKOP_CLS_CODE` 표의 코드값 미기재 항목("단일가개시", "서킷브레이커CB 단일가접수")의 실제 코드값은 무엇입니까?** `AF8`/`AF1`/`BF9` 가 이에 해당합니까?

3. **`ISCD_STAT_CLS_CODE` 가 `H0STMKO0` 에서 항상 `57` 로 수신되는데, 이 값의 정확한 의미는 무엇입니까?** 동일 종목·시각의 KRX 종목상태 마스터 값(`00`/`23`/`50`/`76` 등)과 다른 이유(필드 의미·소스 차이)를 안내 부탁드립니다.

4. **`EXCH_CLS_CODE`(거래소구분코드)의 가능한 값과 의미는 무엇입니까?** 개발문서에 값 목록이 없습니다. (KRX/NXT/통합 구분 여부 포함)

5. **`ANTC_MKOP_CLS_CODE` 의 `311`/`112` 가 장중 VI 단일가·서킷 구간에 수신되는 경우의 정확한 의미는 무엇입니까?** ("장전예상시작/장전예상종료"라는 문서 설명과 시간대가 일치하지 않습니다.)

6. **매도/매수 사이드카(187/388/397/398)는 `H0STMKO0` 로 개별 종목 단위 전송됩니까?**
   2026-06-26 11:12 매도 사이드카 발동 당시 코스피·코스닥 종목을 `H0STMKO0` 로 구독 중이었음에도 약 5분간 어떤 데이터도 수신되지 않았습니다. 사이드카는 개별 종목 매매상태를 변경하지 않아 이 항목으로는 전송되지 않는 것인지, 전송된다면 어떤 조건/종목으로 오는지, 또는 사이드카 감지를 위해 **별도 TR/실시간 항목**이 필요한지 안내 부탁드립니다.

7. **(요청) 개발문서 `H0STMKO0` 응답 필드의 코드표를 현행 운용값 기준으로 최신화**해 주시기 바랍니다. (A/B 접두 코드군, `EXCH_CLS_CODE` 값, 미기재 항목 보강)

---

## 4. 수신 로그 (증빙)

수신 종목: 다스코(058730, 유가증권/코스피). 당일 VI 다수 발동 + 12:10 서킷브레이커로 거래정지된 종목.

### 4-1. `MKOP_CLS_CODE` = `AF8`(서킷 발동) / `AF1`(서킷 해제)
```
2026-06-26 12:10:12 | [장운영정보] 다스코(058730) mkop_cls_code=AF8 antc_mkop_cls_code=112 trht_yn=Y
2026-06-26 12:30:12 | [장운영정보] 다스코(058730) mkop_cls_code=AF1 antc_mkop_cls_code=112 trht_yn=N
```
- 같은 레코드의 `TRHT_YN`: 12:10 발동 `Y` → 12:30 해제 `N` 정상 동작(필드 정렬 정상 근거).

### 4-2. `MKOP_CLS_CODE` = `BF9` (서킷 이후 VI 단일가 추정 구간, 평시 trht_yn=N)
```
2026-06-26 12:40:34 | [장운영정보] 다스코(058730) mkop_cls_code=BF9 antc_mkop_cls_code=311 trht_yn=N
2026-06-26 12:54:34 | [장운영정보] 다스코(058730) mkop_cls_code=BF9 antc_mkop_cls_code=112 trht_yn=N
2026-06-26 12:57:03 | [장운영정보] 다스코(058730) mkop_cls_code=BF9 antc_mkop_cls_code=311 trht_yn=N
2026-06-26 13:46:45 | [장운영정보] 다스코(058730) mkop_cls_code=BF9 antc_mkop_cls_code=112 trht_yn=N
2026-06-26 13:48:58 | [장운영정보] 다스코(058730) mkop_cls_code=BF9 antc_mkop_cls_code=311 trht_yn=N
```

### 4-3. 평시 `MKOP_CLS_CODE`=None, `ANTC_MKOP_CLS_CODE`=311/112 (분포)
```
mkop_cls_code=None antc_mkop_cls_code=311  (22건)
mkop_cls_code=None antc_mkop_cls_code=112  (8건)
```

### 4-4. `ISCD_STAT_CLS_CODE`=57(H0STMKO0) ↔ KRX 종목상태 마스터 불일치
```
H0STMKO0 iscd_stat=57  vs  KRX(00)  9건
H0STMKO0 iscd_stat=57  vs  KRX(23) 10건
H0STMKO0 iscd_stat=57  vs  KRX(50) 11건
H0STMKO0 iscd_stat=57  vs  KRX(76) 14건
(그 외 KRX 02/06/19/60/70/80 등)
```

### 4-5. 서킷 구간 전 종목 동시 거래중단 정황 (코스피 체결 무수신)
유가증권시장(코스피) 6종목이 12:10:12 동시에 체결 수신이 끊겼다가 12:30 동시 재개 (코스닥 종목은 정상 거래 지속):
```
12:15:15 무수신: 다스코(058730:303s), 광주신세계(037710:319s), 금호전기(001210:314s),
                 금호건설(002990:304s), 금호타이어(073240:303s), SK이터닉스(475150:303s)
12:30:16 무수신: 위 동일 6종목(1204s 경과) → 직후 체결 재개
```
(303초 전 = 12:10:12, 1204초 전 = 12:10:12)

### 4-6. 매도 사이드카(11:12) — 수신 없음
- 11:12:12 매도 사이드카 발동. 당시 `H0STMKO0` 구독: 코스피=다스코(058730), 코스닥=오에스피(368970).
- **11:00 ~ 12:00 구간 `H0STMKO0` 장운영정보 푸시 0건** (사이드카 187/388 등 미수신).
